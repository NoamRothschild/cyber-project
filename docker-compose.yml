services:
  redis:
    image: redis/redis-stack:latest
    container_name: redis-auth-session
    ports:
      - "6379:6379"   # Redis database port
      - "8001:8001"   # Redis Insight Web UI (optional)
    restart: always